*.vs
*.vscode
profiling/
*.idea

install/
build/
deps/
docs/

imgui.ini
imnode-window.json

# Claude configuration is tracked; only local overrides and scratch are not.
# The !.claude/docs/ negation must follow the docs/ rule above to win.
!.claude/docs/
.claude/settings.local.json
.claude/temp/*
!.claude/temp/.gitkeep
